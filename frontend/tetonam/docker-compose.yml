services:
  frontend:
    build: .
    container_name: tetonam-frontend
    ports:
      - "3000:80"
    # volumes:
    #   # SSL 인증서 마운트 (Let's Encrypt 사용 시)
    #   - /etc/letsencrypt/live/i13e108.p.ssafy.io/fullchain.pem:/etc/ssl/certs/i13e108.p.ssafy.io.crt:ro
    #   - /etc/letsencrypt/live/i13e108.p.ssafy.io/privkey.pem:/etc/ssl/private/i13e108.p.ssafy.io.key:ro
    restart: unless-stopped
    networks:
      - tetonam-network

networks:
  tetonam-network:
    external: true